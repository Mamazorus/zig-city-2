# electron-vite@5 déclare une peer dependency vite "^5 || ^6 || ^7", alors que le
# projet utilise vite 8 (compatible en pratique, le build fonctionne). On tolère ce
# décalage pour que `npm ci` (en local ET sur GitHub Actions) n'échoue pas sur ERESOLVE.
legacy-peer-deps=true
